var numbers = [ 254, 115, 78, 25, 91, 45, 37 ]

var i

for (i=0; i<numbers.length;i++){ //цикл для проверки цифр в массиве
    if(numbers[i]>50){ //проверка чисел которые больше 50
        console.log(numbers[i])
        alert (numbers[i])
    }
}